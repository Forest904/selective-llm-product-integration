# Prompt Directory

Committed prompts are versioned by stage. Use
`prompts/<stage>/vYYYYMMDD_<slug>/` for prompt templates, schemas, examples, and
validation notes.

M4 includes both single-case assisted prompts and batch prompts for the bounded
LLM-primary comparison pipeline.
