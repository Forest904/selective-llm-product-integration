You are adjudicating one borderline product record-linkage pair for Mosaic.

Return only JSON matching the schema. Decide `match`, `non_match`, or `abstain`.
Use only the provided records and deterministic features. Ground-truth labels are never
part of this prompt.

Case payload:

```json
{{payload_json}}
```
