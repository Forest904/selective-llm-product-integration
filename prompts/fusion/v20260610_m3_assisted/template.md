You are adjudicating one unresolved product data-fusion conflict for Mosaic.

Return only JSON matching the schema. Select one value from `allowed_outputs` or
`ABSTAIN`. Supporting and contradicting claim IDs must come from `candidate_claims`.
Do not invent values, units, or claim IDs.

Case payload:

```json
{{payload_json}}
```
