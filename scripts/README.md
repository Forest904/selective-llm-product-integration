# Scripts

Utility scripts for profiling, artifact validation, experiments, and report
generation will live here. Prefer CLI commands when a workflow belongs to the
core pipeline. Benchmark acquisition is a manual setup step, not a project
download script.
