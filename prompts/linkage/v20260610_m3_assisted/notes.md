# Linkage Prompt Notes

This prompt is routed only for blocked candidate pairs inside the configured probability
band. Prompt payloads may include pair features, agreements, conflicts, and normalized
record summaries, but must not include `ground_truth_label`.
